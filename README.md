# Steps to run:
1. from 'src/main/resources/static' run 'npm install'
2. from 'src/main/resources/static' run 'gulp clean-build-app-dev'
3. run 'mvn clean install' on project
4. run 'mvn spring-boot:run'
5. Visit http://localhost:8060
